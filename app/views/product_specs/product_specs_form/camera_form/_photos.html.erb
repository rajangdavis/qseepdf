<div class="col-lg-5">
	<%= form_for(@product_spec, multipart: true) do |f| %>
		<h2 class="">Product Spec Photos</h2>
		<div class="form-group">
			<%= f.label :camera_image %>
			<%= f.file_field :camera_image %>
		</div>
		<div class="form-group">
			<%= f.submit 'Add Pictures' %>
		</div>
	<% end %>
</div>